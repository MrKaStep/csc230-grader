{
    "rules": [
        {
            "name": "static_functions",
            "config": {
                "penalty_step": 0.5,
                "whitelist": {
                    "base": ["skipSpace", "readValue", "writeValue"],
                    "operation": ["plus", "minus", "times", "divide"],
                    "calc": ["main"]
                }
            }
        },
        {
            "name": "required_functions",
            "config": {
                "symbols": {
                    "base": ["skipSpace", "readValue", "writeValue"],
                    "operation": ["plus", "minus", "times", "divide"],
                    "calc": ["main"]
                }
            }
        }
    ]
}

